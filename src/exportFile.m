function exportFile(mzList,intensityMatrix,j)
% Function to export stored peak lists and corresponding intensities to a .csv file
    
    % Deal with export of both positive and negative mode data
    if nargin < 3
      j = 0;
    end

    if j == 0

    else

    end
end
